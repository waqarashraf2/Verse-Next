export const metadata = {
  title: "Admin Panel",
  description: "Secure Verse Next admin panel for project inquiries, quotes, consultations, leads, and site management.",
  robots: {
    index: false,
    follow: false,
  },
};

export default function AdminLayout({ children }) {
  return children;
}
